<div>
	<h1 class="jumbotron">{{post.title}}</h1>
</div>
<hr>
<div class="container">
	<div class="well">
		<div><h3>{{post.body}}</h3></div>
		<small><span>Initiated By: </span><span>{{post.initiatedBy}}</span></small>

	</div>
	<hr>
	<div class="s-center blk4">
		<form action="/{{post.slug}}/close" method='post'>
			<p><input type="text" name="post_closedBy" placeholder="Enter downloader Name" class="form-control"></p>
			<input type="submit" value="submit" class='btn btn-lg btn-success'></input>
		</form>
	</div>
</div>